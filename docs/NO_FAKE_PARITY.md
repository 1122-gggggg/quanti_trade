# No Fake Parity

Do not mark a feature complete when it is only a static panel, random-number demo, localStorage record, hard-coded signal, or inactive toolbar button.

A feature may be labelled `demo` or `foundation` until its data, calculations, persistence, tests and failure behavior satisfy the platform definition of done.
